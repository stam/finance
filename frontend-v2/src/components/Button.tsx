import styled from "styled-components";

export const Button = styled.button`
  background: var(--main);
  padding: 0.5rem 1rem;
  border-radius: 4px;
  border: none;
  cursor: pointer;
  outline: none;
  color: white;
  font-size: 1rem;
`;
