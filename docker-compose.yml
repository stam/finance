version: '3'

services:
  db:
    image: postgres
  web:
    build: backend
    command: python ./manage.py runserver 0.0.0.0:8000
    environment:
      - CY_ENV_FILE=.env
    volumes:
      - ./backend:/backend
    ports:
      - "8000:8000"
    depends_on:
      - db
